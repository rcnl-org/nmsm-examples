# Example title

## Background

What is this example meant to show?

## Methods

This example uses the [tool name(s)] tool(s) which can [relevant tool features]. This should be in a relevant tool's directory.

Describe the data provided and important/relevant settings in XML files. 

## Running this example

Remind users to load the project. 

Point users to which script to run for both running and plotting results for the example. Users should not have to modify any input data, settings files, or scripts for the code to run correctly. Running the example and plotting results should either be two separate scripts or one script with two sections in separate `%%` blocks. These scripts should be as simple as possible. Follow our Matlab style guide. Use helper functions if the scripts need to be long. 

Briefly describe what users should see from plotting results. 

## References (if applicable)